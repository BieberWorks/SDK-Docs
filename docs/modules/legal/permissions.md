# Permissions

| Permission key | Constant | Description |
|---|---|---|
| `legal:admin` | `LegalPermissions.Admin` | List and edit all legal documents in the admin UI |

Register the Legal module to have the `LegalPermissionContributor` automatically added to the
global permission catalogue. Assign `legal:admin` to admin roles in the SDK-Auth roles screen.
